version: '3.8'
services:
  RabbitMQ:
    image: RabbitMQ:latest
    restart: unless-stopped
  blackroad-ui:
    build: ../ui
    ports: ["3000:3000"]
  blackroad-api:
    build: ../api
    ports: ["8080:8080"]
